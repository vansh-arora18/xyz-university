<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/css/home-style.css" />
    <link rel="stylesheet" href="/css/tpp.css" />
  </head>
  <body>
    <%- include("navbar"); -%>
    <div class="container">
      <%- include("sidebar"); -%>
        <div class="content">
            <div class="contentHeading">
                <h2><%=name%></h2>
            </div>
            <% if(success != " "){ %>
              <div class="alert alert-success">
                <h3><%=success%></h3>
              </div>
            <% }%>
          <div class="selectCB">
            <div class="selector">
            <form action= "/deleteSection_sec" method="POST">
            <label for="Section">Section: <span>*</span></label>
            <select name="Section" id="Section" required>
                <% for(const i of section){%>
                <option value=<%=i%>><%=i%></option>
                <%}%>
            </select>
            </div>
            <input type="text" name="value" value="<%= name %>" hidden />
            <input type="text" name="batch" value="<%= batch %>" hidden />
            <button id="submit" type="submit">Delete</button>
            </form>
          </div>
        </div>
    </div>
    <script src="../js/index.js"></script>
  </body>
</html>

